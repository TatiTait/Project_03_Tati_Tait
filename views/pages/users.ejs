
<h1>All users</h1>
    <% for (let i =0; i < users.length; i++) { %>
        <p>
            <strong><%= users[i].firstname + ' ' + users[i].lastname%></strong><br/>
            <%= users[i].email %><br/>
            <%= users[i].password %> 
        </p>    
    <% } %>

